const pow = require('./pow');

test('it should square 3', () => {
  expect(pow(3, 2)).toBe(9);
});

test('it should cube 4', () => {
  expect(pow(4, 3)).toBe(64);
});

test('it should square 3', () => {
  expect(pow(3, 2)).toBe(9);
});
